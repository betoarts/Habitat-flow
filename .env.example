# ============================================================================
# Variáveis de Ambiente - HabitFlow PWA
# ============================================================================

# API do backend de push notifications
VITE_API_URL=http://localhost:3001

# Chave pública VAPID (mesma configurada no backend)
# Gerar com: cd server && npm run generate-vapid
VITE_VAPID_PUBLIC_KEY=sua_chave_publica_aqui

# API Key do Gemini (se utilizado)
API_KEY=sua_api_key_gemini
