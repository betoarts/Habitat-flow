# ============================================================================
# Configuração do Servidor de Push Notifications - HabitFlow
# ============================================================================

# VAPID Keys (gerar com: npm run generate-vapid)
# Execute este comando e copie as chaves geradas para cá:
# npx web-push generate-vapid-keys --json

VAPID_PUBLIC_KEY=sua_chave_publica_aqui
VAPID_PRIVATE_KEY=sua_chave_privada_aqui

# Email de contato (obrigatório pelo protocolo VAPID)
# Usar formato mailto:seu@email.com
VAPID_EMAIL=mailto:contato@habitflow.app

# Porta do servidor
PORT=3001

# URL do cliente (para configuração CORS)
CLIENT_URL=http://localhost:3000
